# 旧工作台不可达代码清理

日期：2026-09-06。实施范围为本地前端修改与本地 Git 提交，不包含推送或部署。

## 删除内容

- `FilmCreate.vue`：无入口的修复流水线、旧单镜头处理链、五个不可达弹窗、三个专用文件输入，以及专属状态、导入和样式。
- 删除恒真 `showLegacyPipeline`，保留剧本阶段的一键流程入口。删除空的 `trackFilmCreateAction` 及包装调用。
- 删除没有生产消费者的 `UniversalSegmentOmniAtEditor.vue` 和 `exportStoryboardSheet.js`。
- 清理 `FilmList.vue` 的旧编辑弹窗、旧导出入口和无用计算属性；清理 `DramaDetail.vue`、`FreeCreate.vue`、`OmniAssetPromptEditor.vue` 的无用局部函数。
- 更新测试中固定旧实现名称的断言，保留当前工作台的行为约束。

验收中修正三处已有样式冲突：删除提示词区重复最小高度；成片操作栏使用安全末端对齐；删除角色卡操作区的禁止换行覆盖。窄屏工作台页头限制为可横向滚动的导航区，避免品牌文字挤压正文。

## 消费者与保留路径

检索范围：`frontweb/src`、`backend-node/src`、`backend-node/test`、`frontweb/test`。检查模板、监听器、生命周期、公开方法及动态类。

| 范围 | 结果 |
| --- | --- |
| 已删除组件、导出工具、空埋点和恒真开关 | 生产代码和测试无残留引用 |
| 一键流程、批量图片/视频、角色/场景/道具提取 | 保留当前入口、共享 composable、任务状态与轮询 |
| 项目分镜、独立全能创作 | 保留 `FreeCreate`、`OmniAssetPromptEditor`、生成参数、素材引用、复制与拖动处理 |
| 画布、资源管理、历史详情 | 保留共享 API、素材协调、历史读取和本地媒体恢复 |
| 后端、鉴权、计费、数据库、供应商适配 | 未修改 |

未引入记录级新行为。新旧记录继续使用已有 API 和恢复语义；没有迁移、重写、清理或付费处理任何历史业务数据。

## 验证结果

- 全部前端测试：133 项通过，0 失败。
- Production build 通过；仍有原有大于 500 kB 的分包提示。
- `git diff --check` 通过。
- 内置浏览器检查首页、剧集页、工作台四阶段和独立创作页，覆盖 1280×720、1440×900、1920×1080；另检查 390×844 窄屏。
- 验证剧本保存、手动添加角色、提示词编辑及刷新恢复、素材库加入、`@` 引用、剧集切换和独立草稿保存。
- 验证成片详情跳转、错误状态预览、缺少镜头时禁止合成，以及工作区滚动和末尾操作可达性。
- 使用独立 SQLite、存储目录、dev 配置和仅允许 loopback 的网络保护。通过项目 HTTP API 提交模拟视频任务，验证处理中、完成、本地 MP4 播放和模拟积分结算。
- 后端重启后，本地成片仍可播放；模拟供应商日志保持一次提交和一次查询，没有重复提交。
- 排序 HTTP API 写入 `[1,3,2,4]` 后，浏览器刷新显示相同顺序。

## 人工复核与既有问题

内置浏览器自动拖动未能确认排序结果。用户随后在本地开发环境试用，确认未发现拖动排序问题，并授权提交。该项以用户人工复核完成验收；现用拖动处理未被本次清理修改，排序接口与刷新读取已经验证。

390 像素首页仍有既有布局问题：主导航文字换行，记录入口与标题区域接近。本轮未扩展为首页移动端重设计。

本轮代码清理、自动化检查和人工复核已完成。
