# 提交前兼容性检查

基于 main 的 `ee7fee7b0101ea191072b8e73d63ec46efb3ac15` 整理修复分支，保留 #32 的 Seedance 比例限制。排除工作区已有启动脚本改动、本地数据和日志。

发现并修复一处历史读取回归：项目软删除后，仍有明确所有者的图片、视频和素材会被新增媒体鉴权拒绝。现在保留原所有者读取能力；其他用户和已删除媒体仍不可读取。HTTP 集成测试覆盖 API、静态文件及数据库重开。

只读复制本地数据库，在副本上模拟旧列结构并连续迁移两次。67 张表的原有字段内容保持一致，完整性检查通过。历史任务的恢复标记保持 0。88 条具有现存本地文件的历史媒体均可由原所有者读取；另有 2 条记录的文件原本缺失，未补写或删除。

新增后处理恢复仅对新建任务、明确重试以及本次明确结算或豁免的恢复标记启用。未标记历史任务保持原路径。导入回滚和临时文件清理仅作用于本次明确创建的文件。迁移增加安全默认值，不批量删除或重写历史数据。

权限收紧会拒绝无权限的全局写入、跨用户资源访问和媒体访问，这是预期安全修复。旧请求字段、状态值及省略新参数时的业务默认值保持兼容。消费者覆盖路由、服务、前端封装、列表/详情、历史读取、计费和重启恢复；详细范围见同目录修复进度。

本分支后端测试 361/361、前端测试 145/145 通过，production build 成功。后端测试禁止非 loopback 网络。空库重复迁移通过。前端交互验收记录见修复进度，覆盖三个要求的桌面视口和窄屏。本次兼容修正没有新增界面改动。

检查范围内未发现剩余破坏性变更。未执行生产操作或真实生成。远端 CI 与预览结果以 PR 对应提交的检查记录为准。
